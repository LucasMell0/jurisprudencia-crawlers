# Postgres do Autosfy (db01) — mesmas tabelas jurisprudence_* que o
# crawler do Jusfy usa (ver adfy/sql/016 e 017). Alcançável por LAN
# (192.168.31.68) ou Tailscale a partir desta VPS.
DATABASE_URL=postgresql://autosfy_app:SENHA@192.168.31.68:5432/autosfy_db
DB_POOL_MAX=10

FALCAO_CONCURRENCY=3
FALCAO_REQUEST_DELAY_MS=300
